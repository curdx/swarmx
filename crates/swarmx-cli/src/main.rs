//! `swarmx up` launcher. Stub for now; in M1 you run `cargo run -p swarmx-server`
//! directly.

fn main() {
    eprintln!("swarmx: use `cargo run -p swarmx-server` until the launcher lands");
    std::process::exit(1);
}
