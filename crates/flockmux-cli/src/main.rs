//! `flockmux up` launcher. Stub for now; in M1 you run `cargo run -p flockmux-server`
//! directly.

fn main() {
    eprintln!("flockmux: use `cargo run -p flockmux-server` until the launcher lands");
    std::process::exit(1);
}
